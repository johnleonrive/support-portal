The problem is likely in the authentication format. Tell Claude Code to test with this exact code:
// Test with correct authentication format
const apiKey = '143684ef3bb7944';
const apiSecret = 'YOUR_SECRET_HERE'; // Replace with actual secret

console.log('Testing authentication...');

try {
  const response = await fetch('https://intern-dev.frappe.cloud/api/method/frappe.ping', {
    method: 'GET',
    headers: {
      'Authorization': `token ${apiKey}:${apiSecret}`,
      'Content-Type': 'application/json',
      'Accept': 'application/json'
    }
  });
  
  console.log('Status:', response.status);
  console.log('Response:', await response.text());
} catch (error) {
  console.error('Error:', error);
}
Common auth format issues:

❌ Bearer ${token}
❌ Api-Key: ${key}, Api-Secret: ${secret}
✅ token ${key}:${secret} (correct format)

Also try the HD Ticket endpoint:
const response = await fetch('https://intern-dev.frappe.cloud/api/resource/HD Ticket', {
  headers: {
    'Authorization': `token ${apiKey}:${apiSecret}`,
    'Content-Type': 'application/json'
  }
});
Can you have Claude Code run these exact tests? This will verify if the authentication format is correct.
The fact that you have fresh API keys should solve the 401 errors!